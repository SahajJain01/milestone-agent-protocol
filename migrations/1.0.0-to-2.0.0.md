---
id: 1.0.0-to-2.0.0
from: 1.0.0
to: 2.0.0
---

# Migrate protocol 1.0.0 to 2.0.0

## Preconditions

- The target has a valid `.agent/protocol.lock.yaml`.
- The lock records protocol version `1.0.0` and entrypoint digest
  `sha256:e8a83c49c1121a7d3b545c8c7c2ff53afd45766ee316fdc207e57bb1a4a09917`.
- Protocol-managed paths can be reconciled without overwriting project-owned content.
- No same-version digest fork or unresolved local divergence exists.

## Operations

1. Reconcile protocol-managed sections to the rule identities declared by 2.0.0, including
   `declared-paths-only`.
2. Preserve repository-specific guidance, immutable IDs, source anchors, decisions, and external
   evidence.
3. Update protocol-managed sections to the 2.0.0 schemas without creating paths outside the current
   manifest.
4. After verification, update `.agent/protocol.lock.yaml` to version `2.0.0`, set
   `last_applied: 2.0.0`, and append `1.0.0-to-2.0.0` once.

## Verification

- Every path declared by `required_target_paths` exists.
- Protocol-managed sections express only current 2.0.0 rule identities.
- Project-owned content and unrelated paths are unchanged.
- Internal links, immutable IDs, source anchors, and target verification pass.
- A repeated 2.0.0 reconciliation produces zero diff.

## Abort behavior

Stop without advancing the lock if a precondition, ownership check, or verification step fails.
Revert only changes attributable to this migration; never use destructive Git operations.
