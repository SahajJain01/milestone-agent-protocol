interface:
  display_name: "Initialize Agent Protocol"
  short_description: "Initialize, adopt, or upgrade agent protocol"
  default_prompt: "Use $initialize-agent-protocol to initialize, adopt, or reconcile the agent protocol in this repository."
